#include <stdio.h>
#include <stdlib.h>
/**
 * main - main Entry 
 * Return: 0 
*/
int main(void)
{
	char h;

	for (h = '0'; h <= '9'; ch++)
	{
		putchar(h);
	}
	for (h = 'a'; h <= 'f'; h++)
	{
		putchar(h);
	}
	putchar('\n');
	return (0);
}
