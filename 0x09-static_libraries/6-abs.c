#include "main.h"

/**
 * _abs - Computes the absolute value of n
 * @n: number to be computed
 * Return: absolute value of the integer
 */
int _abs(int n)
{
if (n >= 0)
return (n);
else
return (-n);
}
